{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && vite-gh-pages"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "vite-plugin-gh-pages": "^1.0.1"
  }
}
