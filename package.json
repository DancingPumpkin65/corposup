{
  "scripts": {
    "dev": "cd frontendd && npm run dev",
    "build": "cd frontendd && npm run build",
    "preview": "cd frontendd && npm run preview",
    "deploy": "npm run build && npx gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  }
}
